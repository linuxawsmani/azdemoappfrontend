version: 0.2

phases:
  build:
    commands:
      - echo "Deploying Static Website"

  post_build:
    commands:
      - aws s3 sync . s3://13072026-azdemo-s3-bucket --delete
      - aws cloudfront create-invalidation --distribution-id E2IBA353FBO4IC --paths "/*" --debug
